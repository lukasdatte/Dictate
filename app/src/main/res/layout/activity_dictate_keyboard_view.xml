<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/dictate_keyboard_view"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:clipChildren="false"
    android:clipToPadding="false"
    android:background="@color/dictate_keyboard_background_light">

    <!-- ════════════════════════════════════════════════════════════
         Main button area — MotionLayout-rooted, flat button hierarchy.

         See: docs/plans/2026-05-07 - dictate-keyboard-layout-refactor/
              research/2-keyboard-layout/2-keyboard-layout.reviewed.md §7.2
              docs/decisions/0004-ui-layout-catalog-motionlayout.md §3

         The old vertical LinearLayout + two nested ConstraintLayouts
         (action_row / input_row) are gone (Spec 2 §11.1). All nine buttons
         are now direct children of the MotionLayout; their positions live
         in res/xml/motion_scene_keyboard.xml, NOT here. The only constraints
         in this file are the MotionLayout's own placement inside the parent
         ConstraintLayout.

         Visibility is owned by LayoutCatalog (ADR-0004 §3 / Spec 2 §7.3) —
         every state-driven button keeps an initial android:visibility hint
         (so the layout-preview is sane) but MotionScene PropertySet entries
         set visibilityMode="ignore" on each, so Catalog predicates win at
         render time.
         ════════════════════════════════════════════════════════════ -->
    <!-- Post-cutover hotfix (R1) — paddingBottom restores the bottom safe-area
         the legacy `input_row.layout_marginBottom="16dp"` provided (Spec 2 §11.1
         deleted the container without rebuilding the spacing). Without it, the
         Row-2 chain (trash/space/pause/enter) sits flush against the keyboard's
         bottom edge → gets pushed under the gesture-nav bezel on modern devices.
         Row-1 ↔ Row-2 inter-row gap is restored on trash_btn's layout_marginTop
         in motion_scene_keyboard.xml two_row_state. -->
    <!-- Post-cutover hotfix #R1-DEEP3 — `app:currentState` MUST be set
         declaratively. The Row-2 buttons (`trash_btn`, `space_btn`,
         `pause_btn`, `enter_btn`) have NO inline child constraints in this
         XML; their `Top_toBottomOf=record_pulse_layout` chain lives only
         in `motion_scene_keyboard.xml` `two_row_state`. Without
         `app:currentState`, MotionLayout's internal `mModel` is empty
         until the first `onMeasure` — and `motionSurface.jumpToState(...)`
         called from `ImeViewBackend.render(firstRender=true)` runs BEFORE
         `onAttachedToWindow`, so it merely stamps `mCurrentState=id`
         without populating constraints. ConstraintLayout then falls back
         to placing every unconstrained child at position (0, 0), so
         `trash_btn` lands directly on top of `record_pulse_layout` (the
         "Row 1 ↔ Row 2 overlap on boot" symptom). Recording later
         triggers `requestLayout()` via `BorderGlowAnimation.start()`
         (foreground/background/text writes on `record_btn`), forcing
         `onMeasure`, populating `mModel` from `two_row_state`, and
         snapping the layout into the correct geometry — that is why
         "starting a recording fixes it" but a cold boot does not.
         Setting `app:currentState` makes `mModel` populate on the FIRST
         measure pass — the layout is correct from frame zero. -->
    <!-- Compact-row fix (2026-07) — paddingStart is 16dp, NOT the historic
         72dp. The 72dp block-indent is a Two-Row design element only; the
         single-row (compact) state needs the full row width or its chain
         overflows and buttons overlap. Padding cannot be overridden per
         MotionScene state, so the indent moved into the scene:
         `two_row_state` carries `layout_marginStart="56dp"` on
         `record_pulse_layout` (16 + 56 = the old 72) and `trash_btn`
         start-aligns to it; `single_row_state` overrides both margins away.
         See SingleRowLayoutGeometryTest for the measured-geometry pin. -->
    <androidx.constraintlayout.motion.widget.MotionLayout
        android:id="@+id/main_buttons_cl"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:paddingStart="16dp"
        android:paddingEnd="16dp"
        android:paddingBottom="16dp"
        android:clipChildren="false"
        android:clipToPadding="false"
        app:layoutDescription="@xml/motion_scene_keyboard"
        app:currentState="@+id/two_row_state"
        app:layout_constraintTop_toBottomOf="@id/edit_buttons_keyboard_ll"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <!-- record_pulse_layout — PulseLayout wrapper around record_btn (L7).
             The wrapper is the direct MotionLayout child; record_btn lives
             inside it.

             Post-cutover hotfix #R1-DEEP2 — the inline `layout_marginBottom`
             here is the **boot-state** anchor for the Row 1 ↔ Row 2 16dp
             gap. MotionLayout only applies its ConstraintSet margins after
             the first `transitionToState`; before that, the inline XML
             LayoutParams are authoritative. Without this attribute the
             very first render of Idle has 0dp gap and only later
             transitions (Idle → Recording → Idle, or Send → Idle) pick up
             the 16dp from `motion_scene_keyboard.xml` `two_row_state`. The
             value is duplicated intentionally: the MotionScene carries
             the same 16dp on `record_pulse_layout.marginBottom` in
             `two_row_state` (and derived states inherit) — keeping the
             two values identical is the simplest way to make the boot
             frame look the same as every subsequent transition frame.

             The inline `layout_marginStart="56dp"` is the same boot-state
             convention for the Two-Row 72dp block-indent (16dp container
             padding + 56dp margin — see the main_buttons_cl comment).
             `two_row_state` carries the identical 56dp; `single_row_state`
             overrides it away so the compact row spans the full width. -->
        <net.devemperor.dictate.widget.PulseLayout
            android:id="@+id/record_pulse_layout"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="56dp"
            android:layout_marginBottom="16dp"
            app:pulseCount="3"
            app:pulseDuration="2000"
            app:pulseStartAlpha="0.3"
            app:pulseMaxRadiusFactor="1.4"
            app:pulseStyle="fill">

            <!-- B4-VAL F-29: catalog overrides android:text at render-time
                 via ButtonSlot.textResolver — the per-mode label resolves
                 from LayoutCatalog.KEYBOARD_*_RECORD slots (Spec 2 §8.5).
                 The static text here is the inflation fallback.

                 B-D-1 (dictate-pipeline-render-and-state-unification §5.1
                 + §9.1 OQ-1 Variante A): `maxLines="2"` lets the
                 pipeline-phase label render as two lines —
                 `<stepName>\n<N>/<M> M:SS` (step name above counter).
                 The Idle/Active/Paused labels remain single-line; the
                 button height grows only during Running, which the
                 wrap_content layout absorbs. -->
            <com.google.android.material.button.MaterialButton
                android:id="@+id/record_btn"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="@string/dictate_record"
                android:textSize="14sp"
                android:maxLines="2" />

        </net.devemperor.dictate.widget.PulseLayout>

        <com.google.android.material.button.MaterialButton
            android:id="@+id/resend_btn"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:foreground="@drawable/ic_outline_change_circle_24"
            android:foregroundGravity="center"
            android:minWidth="0dp"
            android:visibility="gone"
            tools:visibility="visible" />

        <!-- secondary_record_btn — ADR-0009 secondary-recording mic button.
             Rendered only in the SEND_MODE layouts while a pipeline run
             processes; a tap starts a NEW recording that queues behind the
             active run. Static mic foreground (no iconResolver, never
             swapped) mirrors resend_btn / backspace_btn — the LayoutCatalog
             RECORD_SECONDARY slot owns visibility only. Position lives in
             motion_scene_keyboard.xml (between resend_btn and backspace_btn);
             visibilityMode="ignore" is carried by the MotionScene PropertySets
             like every other state-driven button. -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/secondary_record_btn"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:foreground="@drawable/ic_baseline_mic_24"
            android:foregroundGravity="center"
            android:contentDescription="@string/dictate_secondary_record_description"
            android:minWidth="56dp"
            android:visibility="gone"
            tools:visibility="visible" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/backspace_btn"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:foreground="@drawable/ic_baseline_keyboard_backspace_24"
            android:foregroundGravity="center"
            android:minWidth="56dp" />

        <!-- widget_toggle_btn — opens the floating WIDGET overlay (Spec 3
             OPEN-2 / Phase-1-1.0.2). Catalog hides it during pipeline /
             reprocess-staging; position lives in MotionScene. Placeholder
             icon (change_circle) until B5 ships a dedicated drawable.
             B4-VAL F-29: catalog overrides android:foreground via
             ButtonSlot.iconResolver — see LogicalButtonId.WIDGET_TOGGLE
             slot definitions (Spec 2 §8.1 / §8.2). -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/widget_toggle_btn"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:foreground="@drawable/ic_outline_change_circle_24"
            android:foregroundGravity="center"
            android:minWidth="56dp"
            android:visibility="gone"
            tools:visibility="visible" />

        <!-- audio_focus_btn — DO NOT add android:foreground here. The
             volume-on/off flip is owned by the catalog
             `iconResolver = ::resolveAudioFocusIconForSlot` and written by
             SlotRenderer.applySlotToView via MaterialButton.icon — the
             single writer (a static foreground double-draws over it, the
             same B-B pitfall as pause_btn/enter_btn below; the stale
             volume_off foreground was removed in the 2026-07 compact-row
             fix). `iconGravity="textStart"` centers the icon-only
             glyph; the 8dp paddings + zero iconPadding guarantee the 24dp
             icon fits the 40dp compact width `single_row_state` assigns. -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/audio_focus_btn"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:paddingStart="8dp"
            android:paddingEnd="8dp"
            app:iconPadding="0dp"
            app:iconGravity="textStart"
            android:minWidth="56dp"
            android:visibility="gone"
            tools:visibility="gone" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/trash_btn"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:foreground="@drawable/ic_baseline_delete_24"
            android:foregroundGravity="center"
            android:minWidth="0dp"
            android:visibility="gone"
            tools:visibility="visible" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/space_btn"
            android:layout_width="0dp"
            android:layout_height="wrap_content" />

        <!-- pause_btn — DO NOT add android:foreground here. The pause/mic
             icon flip is owned by the catalog `iconResolver = ::resolvePauseIcon`
             (LayoutCatalog.kt:146 / :197 / :578) and written by
             SlotRenderer.applySlotToView via MaterialButton.icon — the single
             writer. A static `android:foreground` would render on top of the
             dynamic icon (Android draws foreground over MaterialButton.icon),
             producing the B-B duplication bug
             (see docs/plans/2026-05-21 - dictate-pipeline-render-and-state-unification §4.2). -->
        <!-- iconGravity="textStart" centers the icon-only glyph; 8dp
             paddings + zero iconPadding fit the 24dp MaterialButton.icon
             into the 40dp compact width single_row_state assigns (2026-07
             compact-row fix); minWidth 56dp keeps the two-row width
             consistent with enter_btn/backspace_btn. -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/pause_btn"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:paddingStart="8dp"
            android:paddingEnd="8dp"
            app:iconPadding="0dp"
            app:iconGravity="textStart"
            android:minWidth="56dp"
            android:visibility="gone"
            tools:visibility="visible" />

        <!-- enter_btn — DO NOT add android:foreground here. The Return /
             Send / Search / Check / NEXT icon flip is owned by the
             catalog `iconResolver = ::resolveEnterIcon` (Plan
             2026-05-23 - dictate-enter-button-host-action) and written
             by SlotRenderer.applySlotToView via MaterialButton.icon —
             the single writer. A static `android:foreground` would
             render on top of the dynamic icon (same B-B duplication
             pitfall as pause_btn — see Spec 2 §4.2 / sibling comment
             on pause_btn above). -->
        <!-- iconGravity="textStart" centers the icon-only glyph; 8dp
             paddings + zero iconPadding fit the 24dp MaterialButton.icon
             into the 40dp compact width single_row_state assigns (2026-07
             compact-row fix). -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/enter_btn"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:paddingStart="8dp"
            android:paddingEnd="8dp"
            app:iconPadding="0dp"
            app:iconGravity="textStart"
            android:minWidth="56dp" />

    </androidx.constraintlayout.motion.widget.MotionLayout>

    <!-- C15 — Removed `action_row` / `input_row` scaffold stubs
         (C13 deviation). The KeyboardLayoutModeController + all
         remaining legacy `findViewById(R.id.action_row|input_row)`
         resolution sites were deleted in Spec 2 §11.8 5d. -->

    <!-- ════════════════════════════════════════════════════════════
         The state-derived info bar (ADR-0006).

         The SINGLE info-bar surface of the keyboard: InfoBarRenderer
         observes `InfoBarSelector.select(state)` and renders the top
         item (onboarding explainer, pending-insert, recovery info,
         pipeline errors, Update/Rate/Donate hints) into these views.
         Visibility + text + button wiring are fully owned by
         InfoBarRenderer — nothing else writes to this block.

         2026-07-02 consolidation (ADR-0006 completion, pending task
         #149): the legacy imperative controller container that used to
         sit above this block is deleted together with its controller
         — one container, one constraint chain. This container heads
         the vertical chain (parent-top, `prompts_keyboard_cl` chained
         below) so a shown info bar pushes the keyboard content down
         instead of floating over it (Z-order fix 2026-05-22
         preserved). The `infobar_*` ids replace the historical
         `overlay_permission_*` names from the block's first life as
         the onboarding-only surface (Spec 3 §5.3, B5 F-2).
         ════════════════════════════════════════════════════════════ -->
    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/infobar_cl"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginHorizontal="16dp"
        android:layout_marginTop="16dp"
        android:visibility="gone"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:visibility="visible">

        <TextView
            android:id="@+id/infobar_message_tv"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginHorizontal="8dp"
            android:text="@string/overlay_perm_explainer"
            android:textColor="@color/dictate_blue"
            android:textSize="16sp"
            android:textStyle="italic"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toStartOf="@+id/infobar_confirm_btn"
            app:layout_constraintHorizontal_bias="0.5"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <Button
            android:id="@+id/infobar_confirm_btn"
            style="@style/Widget.AppCompat.Button.Small"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="@android:color/transparent"
            android:foreground="@drawable/ic_baseline_check_circle_outline_24"
            android:foregroundGravity="center"
            android:contentDescription="@string/overlay_perm_grant"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toStartOf="@+id/infobar_dismiss_btn"
            app:layout_constraintHorizontal_bias="0.5"
            app:layout_constraintStart_toEndOf="@+id/infobar_message_tv"
            app:layout_constraintTop_toTopOf="parent" />

        <Button
            android:id="@+id/infobar_dismiss_btn"
            style="@style/Widget.AppCompat.Button.Small"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="@android:color/transparent"
            android:foreground="@drawable/ic_baseline_not_interested_24"
            android:foregroundGravity="center"
            android:contentDescription="@string/overlay_perm_later"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintHorizontal_bias="0.5"
            app:layout_constraintStart_toEndOf="@+id/infobar_confirm_btn"
            app:layout_constraintTop_toTopOf="parent" />
    </androidx.constraintlayout.widget.ConstraintLayout>

    <!-- ════════════════════════════════════════════════════════════
         REVIEW PANEL (ADR-0013) — covers the main button grid while an
         ambiguous post-processing turn is under review. Opaque surface +
         elevation so it hides (and blocks touches to) the buttons behind
         it; driven entirely by ReviewPanelRenderer (visibility + text +
         button-enable). GONE by default.
         ════════════════════════════════════════════════════════════ -->
    <!-- HEIGHT (Block D fix): the panel OWNS its height — wrap_content, top-anchored
         BELOW the edit bar (exactly where main_buttons_cl starts, so the visual
         position is unchanged) and fully decoupled from the grid — no top/bottom
         coupling to main_buttons_cl. KEYBOARD_REVIEW_PANEL forces every
         main_buttons_cl slot GONE, so that grid collapses to its padding;
         the old 0dp top+bottom coupling made the panel collapse with it (VISIBLE but
         invisibly flat). Mirrors the history-panel pattern (own height → root
         wrap_content grows the IME window). The inner output scroll is height-capped so
         a long output cannot blow up the window. (Future dock: a drag-resize handle
         reusing Block A's VerticalDragResizeHandler — not wired yet.) -->
    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/review_panel_cl"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:background="?attr/colorSurface"
        android:elevation="8dp"
        android:padding="12dp"
        android:minHeight="@dimen/dictate_review_panel_min"
        android:visibility="gone"
        app:layout_constraintTop_toBottomOf="@id/edit_buttons_keyboard_ll"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        tools:visibility="visible">

        <TextView
            android:id="@+id/review_message_tv"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:textColor="@color/dictate_blue"
            android:textSize="14sp"
            android:textStyle="italic"
            android:maxLines="3"
            android:ellipsize="end"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            tools:text="Did you mean the first or second option?" />

        <!-- wrap_content + capped max so the panel (now wrap_content) sizes to the
             actual output and a very long output scrolls within a bounded region
             instead of blowing up the IME window (Block D). Top-anchored only —
             the buttons row sits below it (top-down flow, unambiguous under
             wrap_content). -->
        <ScrollView
            android:id="@+id/review_output_scroll"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:fillViewport="true"
            app:layout_constrainedHeight="true"
            app:layout_constraintHeight_max="@dimen/dictate_review_panel_output_max"
            app:layout_constraintTop_toBottomOf="@id/review_message_tv"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent">

            <TextView
                android:id="@+id/review_output_tv"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:textColor="?attr/colorOnSurface"
                android:textSize="16sp"
                android:textIsSelectable="false"
                tools:text="This is the resulting text that would be inserted." />
        </ScrollView>

        <TextView
            android:id="@+id/review_refining_tv"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:text="@string/dictate_review_refining"
            android:textColor="?attr/colorOnSurface"
            android:textStyle="italic"
            android:visibility="gone"
            app:layout_constraintBottom_toTopOf="@id/review_buttons_row"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            tools:visibility="visible" />

        <LinearLayout
            android:id="@+id/review_buttons_row"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:layout_marginTop="8dp"
            app:layout_constraintTop_toBottomOf="@id/review_output_scroll"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent">

            <com.google.android.material.button.MaterialButton
                android:id="@+id/review_discard_btn"
                style="@style/Widget.Material3.Button.TextButton"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/dictate_review_discard" />

            <Space
                android:layout_width="0dp"
                android:layout_height="1dp"
                android:layout_weight="1" />

            <com.google.android.material.button.MaterialButton
                android:id="@+id/review_redictate_btn"
                style="@style/Widget.Material3.Button.TonalButton"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginEnd="8dp"
                android:text="@string/dictate_review_redictate" />

            <com.google.android.material.button.MaterialButton
                android:id="@+id/review_insert_btn"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/dictate_review_insert" />
        </LinearLayout>
    </androidx.constraintlayout.widget.ConstraintLayout>

    <!-- ════════════════════════════════════════════════════════════════
         HISTORY PANEL (ADR-0014) — a distinctly taller, opaque, elevated
         surface that temporarily replaces the content area / prompt bar and
         covers the button grid. Unlike the review panel (which overlays only
         main_buttons_cl), this anchors below the edit bar and carries its own
         (screen-fraction) list height, so the root wrap_content grows and the
         IME window becomes taller when the panel opens. Driven by
         HistoryPanelRenderer (container visibility) + an IME-owned Paging list.
         GONE by default. ════════════════════════════════════════════════ -->
    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/history_panel_cl"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:background="?attr/colorSurface"
        android:elevation="8dp"
        android:paddingHorizontal="8dp"
        android:paddingTop="8dp"
        android:paddingBottom="4dp"
        android:visibility="gone"
        app:layout_constraintTop_toBottomOf="@id/edit_buttons_keyboard_ll"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        tools:visibility="visible">

        <TextView
            android:id="@+id/history_panel_title_tv"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:gravity="center_vertical"
            android:text="@string/dictate_history_panel_title"
            android:textStyle="bold"
            android:textSize="15sp"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toStartOf="@+id/history_panel_close_btn"
            app:layout_constraintBottom_toBottomOf="@+id/history_panel_close_btn" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/history_panel_close_btn"
            style="@style/Widget.Material3.Button.IconButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:contentDescription="@string/dictate_history_panel_close"
            app:icon="@drawable/ic_baseline_close_24"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintEnd_toEndOf="parent" />

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/history_panel_rv"
            android:layout_width="0dp"
            android:layout_height="@dimen/dictate_history_panel_list_height"
            android:layout_marginTop="4dp"
            android:scrollbars="vertical"
            app:layout_constraintTop_toBottomOf="@id/history_panel_title_tv"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintBottom_toTopOf="@+id/history_panel_resize_handle"
            tools:listitem="@layout/item_keyboard_history" />

        <!-- Drag-to-resize grab handle (Block A). Full-width touch target at the
             BOTTOM of the panel; dragging it down enlarges the list (growWhenDraggingDown),
             up shrinks it. The RV height is driven imperatively by the IME
             (applyHistoryPanelHeight + VerticalDragResizeHandler), persisted to
             Pref.HistoryPanelHeightDp on release. -->
        <View
            android:id="@+id/history_panel_resize_handle"
            android:layout_width="0dp"
            android:layout_height="@dimen/dictate_history_panel_handle_height"
            android:layout_marginTop="2dp"
            android:background="@drawable/dictate_drag_handle"
            android:contentDescription="@string/dictate_history_panel_resize_handle"
            app:layout_constraintTop_toBottomOf="@id/history_panel_rv"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintBottom_toBottomOf="parent" />

        <!-- Full-text detail (Block B, option B-ii). Shown IN PLACE of the list+handle
             when a row is short-pressed (history_panel.detailSessionId != null). The
             HistoryPanelRenderer toggles list vs detail visibility; the IME loads the
             full text via getFinalOutput and reuses the list Insert/Send handlers; Back
             dispatches CloseDetail. Own capped height (wrap_content, like the review
             panel) so a long text scrolls within bounds. GONE by default. -->
        <androidx.constraintlayout.widget.ConstraintLayout
            android:id="@+id/history_panel_detail_group"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:background="?attr/colorSurface"
            android:visibility="gone"
            app:layout_constraintTop_toBottomOf="@id/history_panel_title_tv"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            tools:visibility="gone">

            <ScrollView
                android:id="@+id/history_panel_detail_scroll"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_marginTop="4dp"
                android:fillViewport="true"
                app:layout_constrainedHeight="true"
                app:layout_constraintHeight_max="@dimen/dictate_history_panel_detail_max"
                app:layout_constraintTop_toTopOf="parent"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintEnd_toEndOf="parent">

                <TextView
                    android:id="@+id/history_panel_detail_tv"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:textSize="15sp"
                    android:textColor="?attr/colorOnSurface"
                    android:textIsSelectable="true"
                    tools:text="The full text of the selected history item." />
            </ScrollView>

            <LinearLayout
                android:id="@+id/history_panel_detail_buttons"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:layout_marginTop="8dp"
                android:gravity="center_vertical"
                app:layout_constraintTop_toBottomOf="@id/history_panel_detail_scroll"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintEnd_toEndOf="parent">

                <com.google.android.material.button.MaterialButton
                    android:id="@+id/history_panel_detail_back_btn"
                    style="@style/Widget.Material3.Button.TextButton"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="@string/dictate_history_panel_back" />

                <Space
                    android:layout_width="0dp"
                    android:layout_height="1dp"
                    android:layout_weight="1" />

                <com.google.android.material.button.MaterialButton
                    android:id="@+id/history_panel_detail_send_btn"
                    style="@style/Widget.Material3.Button.TextButton.Icon"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginEnd="4dp"
                    android:minWidth="0dp"
                    android:insetLeft="0dp"
                    android:insetRight="0dp"
                    android:contentDescription="@string/dictate_windows_send_btn"
                    app:icon="@drawable/ic_baseline_send_24"
                    android:visibility="gone"
                    tools:visibility="visible" />

                <com.google.android.material.button.MaterialButton
                    android:id="@+id/history_panel_detail_insert_btn"
                    style="@style/Widget.Material3.Button.TonalButton"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="@string/dictate_history_panel_insert" />
            </LinearLayout>
        </androidx.constraintlayout.widget.ConstraintLayout>

    </androidx.constraintlayout.widget.ConstraintLayout>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/edit_buttons_keyboard_ll"
        android:layout_width="0dp"
        android:layout_height="36dp"
        android:layout_marginTop="16dp"
        android:orientation="horizontal"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/prompts_keyboard_cl">

        <com.google.android.material.button.MaterialButton
            android:id="@+id/edit_undo_btn"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_marginStart="16dp"
            android:layout_marginEnd="4dp"
            android:foreground="@drawable/ic_baseline_undo_24"
            android:foregroundGravity="center"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toStartOf="@+id/edit_redo_btn"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/edit_redo_btn"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_marginHorizontal="4dp"
            android:foreground="@drawable/ic_baseline_redo_24"
            android:foregroundGravity="center"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toStartOf="@+id/edit_cut_btn"
            app:layout_constraintStart_toEndOf="@+id/edit_undo_btn"
            app:layout_constraintTop_toTopOf="parent" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/edit_cut_btn"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_marginHorizontal="4dp"
            android:foreground="@drawable/ic_baseline_content_cut_24"
            android:foregroundGravity="center"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toStartOf="@+id/edit_copy_btn"
            app:layout_constraintStart_toEndOf="@+id/edit_redo_btn"
            app:layout_constraintTop_toTopOf="parent" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/edit_copy_btn"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_marginHorizontal="4dp"
            android:foreground="@drawable/ic_baseline_content_copy_24"
            android:foregroundGravity="center"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toStartOf="@+id/edit_paste_btn"
            app:layout_constraintStart_toEndOf="@+id/edit_cut_btn"
            app:layout_constraintTop_toTopOf="parent" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/edit_paste_btn"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_marginHorizontal="4dp"
            android:foreground="@drawable/ic_baseline_content_paste_24"
            android:foregroundGravity="center"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toStartOf="@+id/edit_emoji_btn"
            app:layout_constraintStart_toEndOf="@+id/edit_copy_btn"
            app:layout_constraintTop_toTopOf="parent" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/edit_emoji_btn"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_marginStart="4dp"
            android:layout_marginEnd="4dp"
            android:foreground="@drawable/ic_baseline_emoji_emotions_24"
            android:foregroundGravity="center"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toStartOf="@+id/edit_keyboard_btn"
            app:layout_constraintStart_toEndOf="@+id/edit_paste_btn"
            app:layout_constraintTop_toTopOf="parent" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/edit_keyboard_btn"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_marginHorizontal="4dp"
            android:foreground="@drawable/ic_baseline_numbers_24"
            android:foregroundGravity="center"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toStartOf="@+id/edit_history_btn"
            app:layout_constraintStart_toEndOf="@+id/edit_emoji_btn"
            app:layout_constraintTop_toTopOf="parent" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/edit_history_btn"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_marginHorizontal="4dp"
            android:foreground="@drawable/ic_baseline_history_24"
            android:foregroundGravity="center"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toStartOf="@+id/edit_audio_focus_btn"
            app:layout_constraintStart_toEndOf="@+id/edit_keyboard_btn"
            app:layout_constraintTop_toTopOf="parent" />

        <!-- Block 2: audio-focus toggle (Edit-Bar variant). Always visible,
             foreground icon + contentDescription are flipped at runtime via
             MainButtonsController.refreshAudioFocusIcon(enabled). -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/edit_audio_focus_btn"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_marginHorizontal="4dp"
            android:foreground="@drawable/ic_baseline_volume_off_24"
            android:foregroundGravity="center"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toStartOf="@+id/edit_settings_btn"
            app:layout_constraintStart_toEndOf="@+id/edit_history_btn"
            app:layout_constraintTop_toTopOf="parent" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/edit_settings_btn"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_marginHorizontal="4dp"
            android:foreground="@drawable/ic_baseline_settings_24"
            android:foregroundGravity="center"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toStartOf="@+id/edit_numbers_btn"
            app:layout_constraintStart_toEndOf="@+id/edit_audio_focus_btn"
            app:layout_constraintTop_toTopOf="parent" />

        <!-- edit_numbers_btn — SmallMode-Toggle (Click: EditBarController
             onSmallModeToggled, Long-press: onSingleRowModeToggled).
             Relocated 2026-05-22 from its old position between emoji + keyboard
             to the right edge of the bar, between settings and widget-toggle
             (user-requested ordering). -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/edit_numbers_btn"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_marginHorizontal="4dp"
            android:foreground="@drawable/ic_baseline_keyboard_arrow_down_24"
            android:foregroundGravity="center"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toStartOf="@+id/edit_widget_toggle_btn"
            app:layout_constraintStart_toEndOf="@+id/edit_settings_btn"
            app:layout_constraintTop_toTopOf="parent" />

        <!-- edit_widget_toggle_btn — relocated 2026-05-22 from the main
             action row (motion_scene_keyboard.xml widget_toggle_btn) into
             the edit-bar. The main-row view stays in place as a GONE
             structural placeholder (LayoutCatalog WIDGET_TOGGLE slot now
             returns visibilityPredicate { false } in TWO_ROW + SINGLE_ROW).
             Click handler bound by EditBarController.onWidgetToggleClicked. -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/edit_widget_toggle_btn"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_marginStart="4dp"
            android:layout_marginEnd="16dp"
            android:foreground="@drawable/ic_outline_change_circle_24"
            android:foregroundGravity="center"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toEndOf="@+id/edit_numbers_btn"
            app:layout_constraintTop_toTopOf="parent" />

    </androidx.constraintlayout.widget.ConstraintLayout>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/prompts_keyboard_cl"
        android:layout_width="0dp"
        android:layout_height="72dp"
        android:layout_marginTop="16dp"
        android:visibility="gone"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/infobar_cl"
        tools:visibility="visible">

        <LinearLayout
            android:id="@+id/prompt_recording_controls_ll"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:orientation="horizontal"
            android:visibility="gone"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            tools:visibility="visible">

            <com.google.android.material.button.MaterialButton
                android:id="@+id/prompt_rec_indicator_btn"
                android:layout_width="0dp"
                android:layout_height="match_parent"
                android:layout_weight="3"
                android:layout_marginEnd="4dp"
                android:minWidth="0dp" />

            <com.google.android.material.button.MaterialButton
                android:id="@+id/prompt_pause_btn"
                android:layout_width="wrap_content"
                android:layout_height="match_parent"
                android:layout_marginEnd="4dp"
                android:foreground="@drawable/ic_baseline_pause_24"
                android:foregroundGravity="center"
                android:minWidth="36dp" />

            <com.google.android.material.button.MaterialButton
                android:id="@+id/prompt_trash_btn"
                android:layout_width="wrap_content"
                android:layout_height="match_parent"
                android:foreground="@drawable/ic_baseline_delete_24"
                android:foregroundGravity="center"
                android:minWidth="36dp" />

        </LinearLayout>

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/prompts_keyboard_rv"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_marginEnd="12dp"
            android:layout_marginStart="12dp"
            app:layout_goneMarginStart="12dp"
            android:scrollbars="horizontal"
            android:scrollbarSize="1dp"
            app:layout_constraintStart_toEndOf="@id/prompt_recording_controls_ll"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            tools:layout_height="32dp" />

        <include
            layout="@layout/pipeline_progress_view"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent" />

    </androidx.constraintlayout.widget.ConstraintLayout>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/emoji_picker_cl"
        android:layout_width="0dp"
        android:layout_height="400dp"
        android:background="@color/dictate_keyboard_background_light"
        android:padding="16dp"
        android:visibility="gone"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        tools:visibility="gone">

        <TextView
            android:id="@+id/emoji_picker_title_tv"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:text="@string/dictate_emoji_picker_title"
            android:textColor="@color/dictate_blue"
            android:textSize="16sp"
            android:textStyle="bold"
            app:layout_constraintBottom_toBottomOf="@+id/emoji_picker_close_btn"
            app:layout_constraintEnd_toStartOf="@+id/emoji_picker_close_btn"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="@+id/emoji_picker_close_btn" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/emoji_picker_close_btn"
            android:layout_width="0dp"
            android:layout_height="36dp"
            android:foreground="@drawable/ic_baseline_close_24"
            android:foregroundGravity="center"
            android:minWidth="0dp"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <androidx.emoji2.emojipicker.EmojiPickerView
            android:id="@+id/emoji_picker_view"
            android:layout_width="0dp"
            android:layout_height="0dp"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@+id/emoji_picker_close_btn" />

    </androidx.constraintlayout.widget.ConstraintLayout>

    <FrameLayout
        android:id="@+id/qwertz_keyboard_container"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:visibility="gone"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/edit_buttons_keyboard_ll"
        tools:visibility="gone" />

    <LinearLayout
        android:id="@+id/overlay_characters_ll"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginEnd="16dp"
        android:orientation="horizontal"
        android:visibility="gone"
        app:layout_constraintBottom_toBottomOf="@+id/main_buttons_cl"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="@+id/main_buttons_cl"
        tools:visibility="visible"
        tools:layout_width="72dp" />

    <!-- ════════════════════════════════════════════════════════════
         2026-05-23 — minimal-strip view.

         Surfaced by `ContentAreaController` when the user holds the
         floating widget overlay open (`WidgetState.Visible(USER)`).
         All other content containers (main_buttons_cl, qwertz, emoji
         picker, edit-bar, info-bars, prompts) are driven to GONE in
         that same render pass, so the entire IME collapses to this
         thin bar — the user sees the floating widget without the
         keyboard competing for screen space underneath.

         Layout shape — anchored to parent-top/start/end at a fixed
         2dp height. Two dp keeps the visual presence minimal (it
         reads as a divider line) while still giving the root
         ConstraintLayout something concrete to size around: a 0dp /
         GONE root would let Android collapse the IME window
         entirely, and on some OEM skins that triggers an awkward
         "input method has no view" flicker before the next
         touch. The padding-less ascii-strip look matches what the
         user explicitly asked for ("nur einen Strich rendert").

         Z-order: declared last so it draws on top, but in practice
         no other container is visible while it is — its visibility
         is mutually exclusive with the rest of the keyboard.
         ════════════════════════════════════════════════════════════ -->
    <View
        android:id="@+id/keyboard_minimal_strip"
        android:layout_width="match_parent"
        android:layout_height="2dp"
        android:background="@color/dictate_black"
        android:visibility="gone"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:visibility="visible" />

</androidx.constraintlayout.widget.ConstraintLayout>
