# Audit Report: plan-and-api (Block 2, scope: full-block)

**Agent-ID:** B2-AUDIT-PLAN-AND-API
**Date:** 2026-05-15
**Knowledge skills used:** knowledge-reference (loaded; TypeScript-pattern catalog — not applicable to this Kotlin/Java Android subsystem; noted, no findings derived from it)
**Files inspected:** 14 (production: `core/PipelineRunnerSubsystemAdapter.kt`, `core/PipelineNotificationCoordinator.kt`, `core/PipelineActionRouter.kt`, `core/ImePipelineConfigResolver.kt`, `core/DictateInputMethodService.java`, `core/DictatePipelineService.kt`, `state/ModuleServices.kt`, `state/Action.kt`, `state/DictateUiState.kt`, `state/PipelineServiceStubSubsystems.kt`, `state/modules/RecordingModule.kt`, `state/modules/AudioModule.kt`; spec: `1-pipeline-service.reviewed.md` §3/§7.4-7.6/§9.6/§13.3.11/§15.1/§15.1.x/§15.2/§15.3; res: `values/strings.xml`, `values-de/strings.xml`)

## Summary

- Critical: 0
- Important: 1
- Nice-to-have: 3

## AC / Deviation Verdicts (stdout-mirrored)

- **AC-1 (no functional stub refs):** GREEN. `grep StubSubsystems.pipelineRunner|StubSubsystems.notificationCoordinator app/.../core/` → zero functional references; the only `core/` mentions are an import, KDoc, a `bluetoothSco` defensive fallback (C9-C11-scoped, not pipelineRunner/notif), and a comment. Both `pipelineRunner` + `notificationCoordinator` stubs are `@Deprecated(level=WARNING)` test-only. Production `ModuleServices` wires `pipelineRunner = pipelineRunnerSubsystemAdapterImpl` (real C3 adapter) + `notificationCoordinator = notificationCoordinatorImpl` (real C4 coordinator) — `DictatePipelineService.kt:450,473,485,487`. No reachable production stub `Log.w`.
- **AC-2/AC-3 (IME-driven new path real end-to-end):** GREEN. `startRecording()` → `dispatch(StartRecording(target, audioFile, preAllocatedId))` (real non-blank UUID, F-7 satisfied); `stopRecording()` → `captureFreshConfigSnapshot` → `dispatch(StopRecordingAndSend)` (payload-less, FN-4). RecordingModule FSM Idle→Preparing→Active emits `Effect.UpdateNotification(Recording)` on Preparing→Active and `EmitPipelineTrigger` on Active/Paused→Idle for StopRecordingAndSend → `TriggerPipeline` → PipelineModule → C3 `PipelineRunnerSubsystemAdapter.submit` → `ImePipelineConfigResolver.resolveFresh` → `JobExecutor.start` inside the C3 adapter. Notification Recording→Pipeline hand-off is seamless (StopRecordingAndSend deliberately emits no DismissNotification). Coordinator renders §7.6 `[Pause][Stopp][Senden]`/`[Resume][Stopp][Senden]`/`[Abbrechen]` exactly; en/de strings present and correctly oriented (default=en, values-de=de).
- **AC-10 (single-architecture):** GREEN. `grep -rn USE_LEGACY_RECORDING_DRIVE app/src/main` → ZERO (constant + all refs deleted by C7). `grep -n JobExecutor.INSTANCE.start DictateInputMethodService.java` → EXACTLY ONE functional site: `:3222` (`startResumeJob`, the documented RESUME carve-out C6-IMPL-2/C5-IMPL-3, `JobRequest.Resume`, unconditional, well-commented). Imported-audio-file path (`onStartInputView:1933` → `transcribeImportedAudioFileViaOrchestrator`) is genuinely orchestrator-routed via `PipelineAction.TriggerPipeline(sessionId, audioFile)` (MID-W1) → C3 adapter; legacy `:2554` `JobExecutor.start` + dead `JobRequest`/`preAllocatedId` removed. Config field-fidelity verified: the imported-file path reuses the **same** `captureFreshConfigSnapshot` helper as the fresh-recording send-tap (extracted verbatim from the legacy method in C5), reading the imported `audioFile` (set at `onStartInputView:1929` before the call). R-1 silent-config-loss surface is closed.
- **C5 record-button-flip deviation:** CONFIRMED spec-faithful. Flipping `startRecording`/`stopRecording` (not the post-record `:2236` callback) is mandated by Spec 1 §15.2 (`StartRecording → AllocateMediaRecorder → RecordingManager.start(file)`) + AC-2 (`state.recording` Idle→Preparing→Active = orchestrator owns the whole recording). Dispatching at the legacy callback would re-allocate a 2nd MediaRecorder over recorded audio. `isEffectiveRecording*`/`cancelEffectiveRecording`/`togglePauseEffectiveRecording` helpers correctly discriminate new vs legacy path via `pipelineBinder != null`. Documented + marker `plan-deviation-resolved`. No unjustified drift.
- **C6-W1 audio-focus/BT-SCO repair:** CONFIRMED spec-faithful. Spec 1 §15.1 row 3 explicitly prescribes the AudioModule observer arm `Recording.Preparing → AudioFocus-Request` ("ja (AudioFocus-Loss → Recording.Pause; Recording.Preparing → AudioFocus-Request)"). The §15.3 example-code S-4 KDoc that *removed* this arm did so on the demonstrably-false premise that `RecordingHardwareAdapter.allocate` requests focus (it provably only `setAudioSource` + `prepare`). The repair restores the §15.1-row-3 arm in AudioModule (the `audio`-axis owner, correct SRP), ADR-0002 Mode-2 cascade → Mode-1 effect, no Mode-3. BT-SCO deferred-allocate (`awaitingSco`/`target` default-valued `Preparing` fields + `ScoRouteResolved` action + Preparing arm) is the minimal spec-faithful legacy-parity realisation. Audio-focus gated on `audioFocusEnabledPref` (legacy `Pref.AudioFocus` default-true parity); release idempotent on stop/pause/cancel.
- **C7 RESUME carve-out + imported-file MID-W1:** CONFIRMED. RESUME byte-identical `if/else` collapsed to the single unconditional `JobExecutor.start` it already contained (behaviour-preserving, not fenced/deleted/rerouted — exactly the carve-out mandate). Imported-file MID-W1 reuses the existing documented `TriggerPipeline` entry-point (no redundant new Action) — smaller + more spec-faithful than C7-IMPL-1's worst-case estimate. Issue Index reconciled correctly (C7-IMPL-1 fixed, C6-IMPL-2 fixed, AC-10 fully GREEN modulo documented RESUME exception).
- **Stub/TODO/NotImplemented introduced by B2 diff:** NONE. The two `UnsupportedOperationException` throws in `DefaultPipelineConfigResolver.resolveFresh` / `ImePipelineConfigResolver.resolveFresh` are intentional R-1 fail-loud guards (surfacing > silent data loss), not unfinished stubs — both are unreachable on the production path (the IME always snapshots before dispatch).

## Findings

### AUDIT-PLAN-AND-API-B2-1

- **Severity:** Important
- **File:** `docs/plans/2026-05-07 - dictate-keyboard-layout-refactor/research/1-pipeline-service/1-pipeline-service.reviewed.md` §15.1.x Coupling-Matrix (line ~6252, the `Audio` owner row) + §15.3 AudioModule example (lines ~6801-6831)
- **Description:** The B2-C6-W1 repair correctly identified §15.1 row 3 as the spec-faithful source and implemented the AudioModule observer arm against it, but the **spec file's own §15.1.x Coupling-Matrix and §15.3 example code were never updated to match**. (a) The §15.1.x matrix `Audio` row still reads only `Recording = R(state.audio.audioFocusGranted) C(RecordingAction.PauseRecording)`. The shipped `AudioModule.onCrossModuleStateChange` now also reads `state.recording` (engagement edge + Preparing.awaitingSco + bluetoothSco.phase) and cascades `Action.AudioAction.RecordingStarted`/`RecordingEnded` and `Action.RecordingAction.ScoRouteResolved`. §15.1.x explicitly states "**Ein neuer Lese-Hook ohne Matrix-Eintrag ist ein Code-Review-Verstoß**" — the `Audio → Recording` cell needs `R(state.recording) R(state.audio.bluetoothSco) C(AudioAction.RecordingStarted) C(AudioAction.RecordingEnded) C(RecordingAction.ScoRouteResolved)` added. (b) The §15.3 example-code KDoc (lines 6811-6817) still asserts the now-disproven S-4 premise ("AudioFocus-Request läuft als Effect direkt im RecordingModule … Effect.AllocateMediaRecorder kapselt das im RecordingHardwareSubsystem.allocate-Pfad — kein Cross-Module-Cascade nötig"). A future reader of the spec will be actively misled into believing the repair is a spec violation when it is in fact §15.1-row-3-faithful. This is exactly the block-report's own self-flagged **Dev-W1-3** (`flagged-for-validate`, "matrix update is a follow-up doc task").
- **Why it matters:** Spec-vs-code drift on a load-bearing SRP-review artefact (the Coupling-Matrix is the documented primary carrier of the cross-module contract per §15.1.x; the §15.3 stale KDoc directly contradicts the shipped, gate-RE-GATE-GREEN-verified design). Left unreconciled, the next plan/review cycle re-litigates a settled, correct decision.
- **Suggested fix scope:** small (doc-only, two spec edits — one matrix cell, one KDoc paragraph; no code change)
- **Suggested fix:** Update the §15.1.x matrix `Audio` owner row `× Recording` cell to add the new reads + cascades; replace the §15.3 lines 6811-6817 S-4 "Dead-Code / kein Cross-Module-Cascade nötig" paragraph with the restored-arm reality (point it at §15.1 row 3 + `research/recording-audiofocus-btsco-handshake.md`). The block-report already tracks this as Dev-W1-3; this finding promotes it from `flagged-for-validate` to an actionable doc-reconciliation item so it is not lost at block close.

### AUDIT-PLAN-AND-API-B2-2

- **Severity:** Nice-to-have
- **File:** `app/src/main/java/net/devemperor/dictate/core/DictateInputMethodService.java:3371-3374` (`handleReprocessSend` new-path branch) and `:2519-2523` (`transcribeImportedAudioFileViaOrchestrator`)
- **Description:** When the new-path is selected but the service is not bound (`pipelineBinder == null || imePipelineConfigResolver == null`), `handleReprocessSend` calls `showJobBusyToast()` — i.e. it tells the user "a job is already active" when the actual condition is "the service is not yet bound". `transcribeImportedAudioFileViaOrchestrator` handles the same not-bound condition correctly (`R.string.dictate_service_not_ready`). The two new-path entry-points are inconsistent for the identical not-ready precondition, and the reprocess one shows a semantically-wrong message. (The block-report documents the reprocess route as "faithful to `handleReprocessSend`", but the misleading-toast is a pre-existing-pattern carry-over, not a fidelity requirement.)
- **Why it matters:** Minor UX correctness + cross-chunk inconsistency (C5 reprocess vs MID-W1 import for the same not-bound state). Not a behaviour/AC regression (both bail without crash/double-dispatch).
- **Suggested fix scope:** small (one-line: swap `showJobBusyToast()` → `dictate_service_not_ready` toast in the `pipelineBinder == null` arm of `handleReprocessSend`, matching the import path)
- **Suggested fix:** Use `R.string.dictate_service_not_ready` for the not-bound branch; keep `showJobBusyToast()` only for the genuine `ActiveJobRegistry.isAnyActive()` branch.

### AUDIT-PLAN-AND-API-B2-3

- **Severity:** Nice-to-have
- **File:** `app/src/main/java/net/devemperor/dictate/state/Action.kt:189` (`PipelineAction.TriggerPipeline`) vs the imported-file caller `DictateInputMethodService.java:2556-2558`
- **Description:** The MID-W1 design routes the imported-audio-file submit through `PipelineAction.TriggerPipeline(sessionId, audioFile)`. `TriggerPipeline`'s KDoc still reads "Initiate a pipeline run for the **just-recorded** audio." — it is now also the imported-file (no-recording-FSM) entry-point. The action is genuinely source-agnostic `(sessionId, audioFile)` (the MID-W1 research correctly argues this), but the KDoc no longer documents the second, equally-valid caller. A reader tracing the imported-file path will not find it referenced from the action that carries it.
- **Why it matters:** Documentation completeness (the SSoT for "who triggers a pipeline" should name both callers — recording-FSM `EmitPipelineTrigger` and the imported-file path). No behaviour impact.
- **Suggested fix scope:** small (KDoc-only — one sentence on `TriggerPipeline` noting the source-agnostic dual use: recording-FSM emit + imported-audio-file path, cross-ref `transcribeImportedAudioFileViaOrchestrator` + `research/imported-audiofile-orchestrator-route.md`)
- **Suggested fix:** Append to the `TriggerPipeline` KDoc: it is the source-agnostic pipeline entry; emitted by `RecordingModule.Effect.EmitPipelineTrigger` (post-record) **and** dispatched directly by the imported-audio-file path (no recording FSM) — both submit a fresh `RECORDING`-kind transcription via the C3 adapter.

### AUDIT-PLAN-AND-API-B2-4

- **Severity:** Nice-to-have
- **File:** `app/src/main/java/net/devemperor/dictate/core/PipelineNotificationCoordinator.kt:333` (consumed) / `state/ModuleServices.kt:333` (`NotificationStatus.Pipeline(sessionId, step: String)`) — C4-IMPL-2 / Dev-3 (already tracked, postponed)
- **Description:** Confirming the already-tracked C4-IMPL-2: `NotificationStatus.Pipeline` carries a `step: String` name, and the coordinator renders the static `dictate_notif_processing` ("Verarbeite …" / "Processing …") subtitle — not the Spec 1 §7.6 `"Verarbeite (Schritt 2/4)"` step counter. This is a documented, deliberately-postponed deviation (the F-13 counter already renders in the record-button label; threading it needs a `NotificationStatus.Pipeline` payload change across every PipelineModule emit-site, out of B2 scope). Raised here only so the plan-and-api topic's §7.6-fidelity coverage is explicit; no new action — agrees with the existing postponement.
- **Why it matters:** Cosmetic notification-subtitle fidelity gap vs §7.6; correctly out of B2 recording-drive scope and already postponed in the Issue Index.
- **Suggested fix scope:** large (cross-cutting `NotificationStatus.Pipeline` payload + every PipelineModule emit-site — Theme-C/follow-up, as already decided)
- **Suggested fix:** None for B2 — confirm the existing postponement stands; owner is Theme-C/follow-up.

## Out-of-scope observations (for the consolidator)

- **(logic)** `AudioModule.reduce(RecordingStarted/RecordingEnded)` returns `nextState = state` (unchanged) with a non-empty `sideEffects` list. The KDoc justifies this (the action is "always relevant; its purpose is the side-effect"). Worth a LOGIC-topic glance that an unchanged-state + non-empty-effects TransitionResult does not get filtered as a no-op anywhere in the orchestrator dispatch path (the block-report's RE-GATE E2E shadow-AudioManager test asserts the effect *does* fire, so this is most likely sound — flagging only for cross-topic confirmation).
- **(logic)** `captureFreshConfigSnapshot:2377` computes `showResend` from `new File(getCacheDir(), Pref.LastFileName).exists()`, but the imported-file path set `Pref.LastFileName` to a file in `getCacheDir()/audio/` (not cacheDir root) at `onStartInputView:1930`. This is verbatim-extracted legacy behaviour (faithful, not B2-introduced) — noting for LOGIC in case the legacy `showResend` computation was itself subtly wrong for the import path pre-refactor (out of plan-and-api scope: it is field-faithful to the deleted legacy construction, which is the AC-9/R-1 contract this topic checks).
- **(test)** AUDIT-TEST should confirm the R-7 order-dependent shared-singleton flake family (PipelineRunnerSubsystemAdapterTest ×4 + LegacyAudioFileMigrationTest ×1 in uncached full runs) is genuinely the documented Epic §6.3 flake and not masking a B2 regression — the RE-GATE already did this causal analysis (W1 diff touches zero JobExecutor/DB files); flagging for the test-topic to independently corroborate.

## Coverage

- Files audited: all 12 production files in the `17085ca..HEAD` diff + the 6 cited spec sections + 2 strings resources (full-block scope, all 5 chunks C3-B1/C4-B2/C5-B3/C6-D2pre/C7-B3 + waves C6-W1 + C7-MID-W1).
- Files skipped (with reason): test files (`*Test.kt`) — out of plan-and-api topic (AUDIT-TEST owns test-quality/coverage); `DictatePipelineService.kt` read only around the Step-3/4 wiring (the parts the AC-1/AC-2 contract sweep needs) — the FGS-lifecycle internals are CONVENTION/LOGIC topic scope.
- Knowledge-skill checkpoints applied: knowledge-reference loaded per the topic catalog; its plugin-system/versioned-envelope patterns are TypeScript-specific and not applicable to this Kotlin/Java subsystem-adapter work (the adapters implement existing interfaces, not a registry/persisted-schema) — no checkpoints derived, documented per the catalog's "if listed, load" rule.
